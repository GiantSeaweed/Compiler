struct A{
  int a;
};

struct Product
{
  int type;
  int name;
  struct A aa;
};

int main()
{

  struct Product cola;
  cola.type = read();
  cola.name = 3;
  write(cola.aa.a + cola.type + cola.name);
  return 0;
}